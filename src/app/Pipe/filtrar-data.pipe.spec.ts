import { FiltrarDataPipe } from './filtrar-data.pipe';

describe('FiltrarDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarDataPipe();
    expect(pipe).toBeTruthy();
  });
});
